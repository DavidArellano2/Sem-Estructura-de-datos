#include<stdio.h>
#include<stdlib.h>
#include<conio.h>
#include<iostream>
#include<math.h>

int main()
    define r1 500
	define r2 200
	define r3 100
	define f2 50
	define f1 25

	int r1,r2,r3,f1,f2,total, adescuento
{int opcion;
ventas==0
    printf("Escribe el numero del producto que vendiste");
    printf("1. Ramo grande $500",r1);
	printf("2. Ramo mediano $200",r2);
	printf("3. Ramo chico $100",r3);
	printf("4. Flor suelta exotica $50 cada una",f1);
	printf("5. Flor suelta tradicional $25 cada una",f2);
	printf("6. Si has terminado");
	cin>>opcion
	if(opcion==1)
        (ventas+500)
        scanf(%d,&r1);
    if(opcion==2)
        (ventas+200)
        scanf(%d,&r2);
    if(opcion==3)
        (ventas+100)
        scanf(%d,&r3);
    if(opcion==4)
        (ventas+50)
        scanf(%d,&f1);
    if(opcion==5)
        (ventas+25)
        scanf(%d,&f2);
    total=(ramo1*r1)+(ramo2*r2)+(ramo3*r3)+(florE*f1)+(florT*f2);
    printf("El total de ventas fue: %d",total);
    if(total>=500)
        cout<<"se aplica el descuento de 5%";
    descuento=total*5/100;
    if(total<=1000);
        cout<<"se aplica el descuento de 5%";
    descuento=total*5/100;
    if(total>=1001);
        cout<<"Se agrega 2% a monedero electronico";
        system(pause)
}
